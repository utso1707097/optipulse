# Base URL of the OptiPulse API. Leave unset for local development (same-origin / dev proxy).
# In a deployed build this MUST be set, because the dashboard and API are on different origins.
# The API must also list this dashboard's origin in Cors__AllowedOrigins.
VITE_API_URL=https://optipulse-api.example.com
