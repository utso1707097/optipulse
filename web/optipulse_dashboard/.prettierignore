# GENERATED — do not let Prettier touch these.
#
# src/api/ is produced by ../../contracts-gen/generate.sh with a PINNED openapi-typescript, and
# the CI drift gate re-runs that generator and fails on any diff. Reformatting the output would
# make `npm run format` a guaranteed CI failure that has nothing to do with the contract.
src/api/schema.d.ts

dist/
node_modules/
package-lock.json
